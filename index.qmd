---
title: "My First Quarto Site"
author: "Austin Rice"
format: html
---

## Welcome

This page is written in **Quarto** and is published straight to GitHub.

- It renders to a real website
- The source is plain text, so git tracks every change cleanly

```{python}
print("Hello from a rendered code chunk!")
```

## Load

```{python}
import pandas as pd
import plotly.express as px

sales = pd.read_csv("data/campus_cafe_sales.csv")

sales.head()
```

```{python}
#| echo: false

print("This line was produced by Python when the document was made, but the code is hidden.")
```

```{python}
#| include: false

# Runs silently, but sales remains available for all chunks below it
sales = pd.read_csv("data/campus_cafe_sales.csv")
```

## Inspect

```{python}
print("Rows and columns:", sales.shape)
print("Cafes:", sales["cafe"].unique())

sales.dtypes
```

## Calculate Revenue and Filter by Coffee

```{python}
sales["revenue"] = sales["units_sold"] * sales["unit_price"]

coffee = sales[sales["category"] == "Coffee"]

coffee.head()
```

## Plot

```{python}
daily = coffee.groupby(
    ["date", "cafe"],
    as_index=False
)["revenue"].sum()

px.line(
    daily,
    x="date",
    y="revenue",
    color="cafe",
    markers=True,
    title="Daily Coffee Revenue by Cafe",
    labels={
        "date": "Date",
        "revenue": "Revenue ($)"
    }
)
```

::: {.callout-note}

## Why I Filtered to Coffee

I filtered the data to coffee so I could compare revenue for the same product category across the three cafes without mixing in products with different prices and sales patterns.

:::

